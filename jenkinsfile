pipeline {
    agent any

    stages {
        stage("build") {
            steps {
                echo "Restoring NuGet packages"
                bat "dir"
                bat "dotnet restore"
                echo "Build solution"
                bat "dotnet build --configuration Release"
            }
        }
    }
}