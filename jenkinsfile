pipeline {
    agent any

    stages {
        stage("nuget") {
            echo "Restoring NuGet packages"
            bat "dotnet restore"
        }

        stage("build") {
            steps {
                echo "Build solution"
                bat "dotnet build --configuration Release"
            }
        }
    }
}