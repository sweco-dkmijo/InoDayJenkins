pipeline {
    agent any

    stages {
        stage("Restoring NuGet packages") {
            echo "Restoring NuGet packages"
                bat "dotnet restore"
        }

        stage("build") {
            steps {
                echo "Build solution"
                bat "dotnet build --configuration Release"
            }
        }
    }
}